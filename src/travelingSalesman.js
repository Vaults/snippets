/**
 * Calculates a (hopefully optimal) tour on an undirected graph. Returns the cities in-order.
 * @param cities Array of distances between cities. : {from: <id_x>, to:<id_y>, dist: int}
 */


const TSP = (distances) => {

};

export {TSP};